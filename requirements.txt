# Pinned to the versions this project was built and tested against. web3 in
# particular is not optional: utilities.py calls contract.encode_abi, which
# exists from web3 7 onwards and is spelled encodeABI before that.
# setuptools 81 drops pkg_resources, which web3's dependency chain still
# imports. Pinned below it, and listed first so it is present before
# anything that builds against it.
setuptools<81

flask==3.1.3
flask-jwt-extended==4.7.4
flask_sqlalchemy==3.1.1
sqlalchemy_utils==0.42.1
mysqlclient==2.2.8
flask_migrate==4.1.0
pymongo==4.17.0
redis==8.1.0
requests==2.34.2
web3==7.16.0
